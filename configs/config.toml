# ==============================================================================
#  🛡️  LoopWarden Configuration
# ==============================================================================

[network]
interface = "eno1"
snaplen = 2048

[alerts]
# Syslog se mantiene simple
syslog_server = ""  # Ej: "192.168.1.50:514"

# --- Webhook (Slack/Teams/Discord) ---
[alerts.webhook]
enabled = false
url = "https://hooks.slack.com/services/..."

# --- SMTP (Email) ---
[alerts.smtp]
enabled = false
host = "smtp.gmail.com"
port = 587
user = "admin@example.com"
pass = "secret"
to = "ops@example.com"
from = "loopwarden@example.com"

# --- Telegram Bot ---
[alerts.telegram]
enabled = false
token = ""
chat_id = ""

[algorithms]

    # --- ALGORITMO 1: EtherFuse ---
    [algorithms.etherfuse]
    enabled = true
    history_size = 4096
    alert_threshold = 50
    storm_pps_limit = 5000

    # --- ALGORITMO 2: ActiveProbe ---
    [algorithms.active_probe]
    enabled = true
    interval_ms = 1000
    ethertype = 65535
    magic_payload = "LOOPWARDEN_PROBE"
    target_mac = "" # Opcional: "FF:FF:FF:FF:FF:FF" para broadcast, o una MAC unicast específica.

    # --- ALGORITMO 3: MAC Storm ---
    [algorithms.mac_storm]
    enabled = true
    max_pps_per_mac = 2000

    # --- ALGORITMO 4: FlapGuard ---
    [algorithms.flap_guard]
    enabled = true
    threshold = 5

    # --- ALGORITMO 5: ArpWatchdog ---
    [algorithms.arp_watch]
    enabled = true
    max_pps = 500
